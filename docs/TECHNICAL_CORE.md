# Planet Pattern — Техническое Ядро

> Реализуемые компоненты без метафизики

---

## 🧩 Что работает (код)

### 1. Волновая память (WaveletMemory)

**Файл:** `wave_memory.py`

**Что делает:**
- Хранит временные ряды как wavelet-коэффициенты (DWT)
- Вместо векторов — волны с ритмом и энергией
- Извлекает центроиды для консолидации

**Применение:**
- HRV-биофидбек (ритм сердца как временной ряд)
- Анализ паттернов речи (эмоции через ритм)
- Персональные агенты с памятью "чувства"

**Код:**
```python
from wave_memory import WaveletMemory
memory = WaveletMemory(window_size=32, wavelet='db2')
memory.push_series(signal_array, meta={'t': timestamp})
centroids = memory.retrieve_centroids(k=8)
```

---

### 2. Формула адаптации E = A × R × L − S

**Файл:** `physics.py`

**Что делает:**
- **A (Attention)**: средняя интенсивность сигнала
- **R (Resonance)**: доля спектральной энергии в полосе 0.1 Hz
- **L (Love)**: корреляция с эталонным сигналом
- **S (Noise)**: энтропия спектра

**Применение:**
- Альтернатива loss-функции для нейросетей (анти-хаос метрика)
- Биофидбек (HRV, дыхание, эмоции)
- Оценка устойчивости системы

**Код:**
```python
from physics import calculate_energy
energy = calculate_energy(signal, reference_wave, fps=1.0)
# Returns: {"A": float, "R": float, "L": float, "S": float, "E": float}
```

---

### 3. Резонансное обучение (PlanetAgent)

**Файл:** `agent.py`

**Что делает:**
- Агент адаптирует параметр `alpha` по резонансу с ритмом
- Обучение через энергию, а не через ошибку
- Мягкое восстановление после провала

**Применение:**
- Адаптивные системы (подстройка под ритм пользователя)
- Децентрализованное обучение (P2P агенты без центра)
- Биофидбек-приложения (дыхание, HRV)

**Код:**
```python
from agent import PlanetAgent
agent = PlanetAgent(name="GaiaLink", alpha=0.5, lr=0.1, adaptive=True)
response = agent.act(phase, local_progress)
agent.learn(coherence_score, target=50.0)
```

---

### 4. LLM-резонансный слой

**Файл:** `llm_resonance.py`

**Что делает:**
- Применяет E = A×R×L−S к текстовым данным
- Адаптирует temperature на основе энергии
- Обратная связь в реальном времени

**Применение:**
- LLM с адаптивным стилем (подстройка под ритм диалога)
- Оценка "живости" ответа модели
- Альтернатива классическому loss

**Код:**
```python
from llm_resonance import LLMResonanceLayer
layer = LLMResonanceLayer(target_hz=0.1)
energy = layer.calculate_llm_energy(
    attention_weights=attn_weights,
    token_times=token_indices,
    token_probs=probs
)
temperature = layer.adapt_temperature(energy["E"])
```

---

## 🎯 Практические применения

| Компонент | Применение | Рынок |
|-----------|-----------|-------|
| **WaveletMemory** | HRV-биофидбек, анализ речи | Wellness, Healthcare |
| **E = A×R×L−S** | Анти-хаос метрика, биофидбек | AI Research, Health Tech |
| **PlanetAgent** | Адаптивные интерфейсы, P2P-ИИ | Gaming, VR, Decentralized AI |
| **LLM Resonance** | LLM с адаптивным стилем | Conversational AI, Therapy |

---

## 📊 Результаты тестов

**Живой агент vs Механический:**
- Живой: `alpha=0.775`, `E=-0.443`, `coherence=3.8%`
- Механический: `alpha=0.500`, `E=-0.553`, `coherence=2.9%`
- **Разница: +0.110 энергии, +0.9% когерентности**

**Вывод:** Адаптивное обучение даёт стабильно лучшие результаты.

---

## 🔧 Технический стек

- **Python 3.11+**
- **NumPy, SciPy** — математика
- **PyWavelets** — wavelet-преобразования
- **Streamlit** — демо-интерфейс

**Зависимости:** `requirements.txt`

---

## 🚀 Что можно сделать дальше

### MVP (1-2 недели)
- Приложение-биофидбек: дыхание ↔ звук ↔ график E
- Интеграция с реальными данными (HRV, микрофон)

### Продукт (1-2 месяца)
- "Resonance Engine" SDK (Python/JS)
- API для встраивания в игры, VR, медитации

### Исследование
- White-paper "Energy-based cognition"
- Заявка на гранты (EU Open Research, Mozilla)

---

## 📚 Документация

- [Архитектура](NGI_vs_AGI.md) — сравнение NGI vs AGI
- [LLM Интеграция](LLM_INTEGRATION.md) — применение к языковым моделям
- [Деплой](DEPLOY_STREAMLIT.md) — инструкции по развёртыванию

---

## 💡 Итого

**Planet Pattern** — это:

1. **Волновая память** для хранения динамики (не только фактов)
2. **Энергетическая метрика** для адаптации (не только loss)
3. **Резонансное обучение** для подстройки под ритм (не только под данные)
4. **LLM-слой** для адаптивного стиля (не только предсказания)

**Без философии. Только код.**

---

**Репозиторий:** https://github.com/Sasha777sh/PlanetPattern  
**Демо:** https://planetpattern-7plrubbahoyhsnzugugmgw.streamlit.app/

